


- `dataset/`

	- `traffic_lights/`
		- `not_traffic_lights/`
			thumbs ready for training
        - `original/`
            plain captchas, downloads or screencaps
        - `processed/`
            all thumbs, the output from `processes_dataset.py`
        - `traffic_lights/`
            thumbs ready for training

	- `crosswalks/`
        - `crosswalks/`
            thumbs ready for training
		- `not_crosswalks/`
			thumbs ready for training
        - `original/`
            plain captchas, downloads or screencaps
        - `processed/`
            all thumbs, the output from `processes_dataset.py`





